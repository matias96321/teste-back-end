
{{#each produtos}}
    <div class="card" style="width: 18rem;">
        <img src="/{{img}}" class="card-img-top" alt="..." style="height: 350px; width: 280px;">
        <div class="card-body">
            <h5 class="card-title">{{produto}}</h5>
            <p class="card-text">{{descrisao}}</p>
            <a href="{{request.url}}" class="btn btn-primary">Configurar</a>
        </div>
    </div>
{{/each}}
</div>