{{#each info}}
<div class="card mb-3">
  <img src="/{{img}}" class="card-img-top" alt="..." style="height: 350px; width: 880px;">
  <div class="card-body">
    <h5 class="card-title">{{produto}}</h5>
    <p class="card-text">{{descrisao}}</p>
    <a href="" class="btn btn-primary">Promoção</a>
    <a href="{{request_editar.url}}" class="btn btn-primary">Editar</a>
    <a href="http://localhost:8080/listProduto" class="btn btn-dark">Voltar</a>
  </div>
</div>
{{/each }}