
<div class="card" style="width: 60%; margin-left: 20%">
  {{#each detalhe}}
    <img src="/{{img}}" class="card-img-top" alt="..." height="350px">
    <div class="card-body">
      <h5 class="card-title">{{produto}}</h5>
      <p class="card-text">{{descrisao}}</p>
      <div class="form-row">
        <label for="">Preço:</label>
        <p class="card-text">{{preco}}</p>
      </div>
      <a href="{{request_editar.url}}" class="btn btn-warning">Comprar</a>
      <a href="http://localhost:8080/home" class="btn btn-dark" >Voltar</a>
    </div>
  {{/each}}
</div>